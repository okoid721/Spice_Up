import React from 'react';
import { motion } from 'framer-motion';
import { Link, useParams } from 'react-hot-toast';
const Search = () => {
    const getSearch = async (name) => {
        const data = await fetch
    }
  return <div></div>;
};

export default Search;
